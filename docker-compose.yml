version: '3'

services:
  app:
    image: kuldeep45/devops-app:latest
    container_name: myapp
    ports:
      - "3000:3000"
    depends_on:
      - db

  db:
    image: mongo
    container_name: mongodb
    ports:
      - "27017:27017"
